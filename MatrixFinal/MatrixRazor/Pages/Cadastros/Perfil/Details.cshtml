@page
@model MatrixRazor.Pages.pgPerfil.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<section class="section-margin">
    <div class="container">
        <div>
            <h2 class="title">Perfil</h2>


            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Perfil.Nome)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Perfil.Nome)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Perfil.Descricao)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Perfil.Descricao)
                </dd>
            </dl>
        </div>
        <div>
            <a asp-page="./Edit" asp-route-id="@Model.Perfil.Id">Editar</a> |
            <a asp-page="./Index">Voltar</a>
        </div>
    </div>
</section>